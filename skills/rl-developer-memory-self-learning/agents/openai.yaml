interface:
  display_name: "RL Developer Memory"
  short_description: "Selective RL/control failure-memory companion for retrieval, guardrails, feedback, and verified durable write-back."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "rl_developer_memory"
      description: "RL-aware MCP surface for compact retrieval, guardrails, preference overlays, and verified write-back."
